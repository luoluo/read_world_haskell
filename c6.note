class Eq a where
    (==), (/=) :: a -> a -> Bool
    x /= y = not (x == y)
    x == y = not (x /= y)

Important Built-In Typeclasses
    * Show
    * Read
    * Maybe

Automatic Derivation
    Read, Show, Bounded, Enum, Eq, Ord

Naming types
    * data
    * type
    * newtype
